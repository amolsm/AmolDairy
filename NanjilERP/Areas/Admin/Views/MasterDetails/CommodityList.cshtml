@model IEnumerable<NanjilERP.Areas.Admin.Models.CommodityListVM>

@{
    ViewBag.Title = "CommodityList";
    ViewBag.Description = "Admin";
}

<!-- modal placeholder-->
<div id='myModal' class='modal fade in'>
    <div class="modal-dialog">
        <div class="modal-content">
            <div id='myModalContent'></div>
        </div>
    </div>
</div>

<!-- Main content -->
<section class="content">


    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Commodity List</h3>
        </div><!-- /.box-header -->
        <div class="box-body">
            <p>
                @Html.ActionLink("Create New", "CommodityCreate", "MasterDetails", null, new { data_modal = "", id = "btnCreate", @class = "btn-xs btn-success" })
            </p>
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Commodity Name</th>
                        <th>Type</th>
                        <th>Is Active</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
            {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.CommodityName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TypesName)
                            </td>
                           
                            <td>
                                @if (item.IsActive == true)
                                {  <text> Yes</text>}
                                else
                                { <text> No</text> }

                            </td>
                            <td>
                                @Html.ActionLink("Edit", "CommodityEdit", new { id = item.CommodityId }, new { data_modal = "" }) |
                                @*@Html.ActionLink("Details", "Details", new { id = item.BrandId }) |*@
                                @Html.ActionLink("Deactive", "CommodityDeactive", new { confirm = true, id = item.CommodityId },
                                            new { onclick = "return confirm('Are you sure to deactive Brand?');" })
                            </td>
                        </tr>
                    }

                </tbody>
                <tfoot>
                    <tr>
                        <th>Commodity Name</th>
                        <th>Type</th>
                        <th>Is Active</th>
                        <th>Action</th>
                    </tr>
                </tfoot>
            </table>
        </div><!-- /.box-body -->
    </div><!-- /.box -->
</section><!-- /.content -->


<script src="~/Scripts/jquery-1.12.4.min.js"></script>
<script src="~/Scripts/ModalForm.js"></script>